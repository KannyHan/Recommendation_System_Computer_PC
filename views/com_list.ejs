<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recommendation System Computer PC for suitable usage</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>
    <div class="bar_home">
        <a href="/">ระบบแนะนำการเลือกคอมพิวเตอร์พีซีให้เหมาะกับการใช้งาน</a>
        <ul>
            <a href="/"><li>หน้าแรก</li></a>
            <a href="/com_list"><li>รายการคอมพิวเตอร์</li></a>
            <a href="/companent"><li>ส่วนประกอบคอมพิวเตอร์</li></a>
        </ul>
    </div>
    <form action="/com_list/search" method="GET">
        <input type="text" name="query" placeholder="ค้นหาคำ..." value="<%= query %>">
        <button type="submit">ค้นหา</button>
        <a href="/com_list/page/1" style="margin-left: 10px;">รีเซ็ต</a> <!-- ปุ่มรีเซ็ต -->
    </form>
        <div class="specFull">
            <h1 style="padding: 15px;border-radius: 5px;color: black;border: 1px solid #ccc;box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);">Computer Specifications</h1>
            <div class="spec-grid">
                <% if (specs.length > 0) { %>
                    <% specs.forEach(spec => { %>
                        <div class="spec">
                            <h2><%= spec.ModelCPU %></h2>
                            <p>Motherboard: <%= spec.ModelMainboard %></p>
                            <p>Graphics Card: <%= spec.ModelVGA %></p>
                            <p>RAM: <%= spec.RAM_Size %> GB</p>
                            <p>SSD: <%= spec.CapacitySSD %> GB</p>
                            <p>HDD: <%= spec.CapacityHDD %> GB</p>
                            <p>Power Supply: <%= spec.PS %> W</p>
                            <p>Case: <%= spec.ModelCASE %></p>
                            <p>Cooling: <%= spec.ModelCOOLING %></p>
                            <p>Monitor: <%= spec.ModelMONITOR %></p>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p>ไม่พบผลลัพธ์ที่ตรงกับคำค้นของคุณ</p>
                <% } %>
            </div>
            
            <div class="pagination">
                <% if (currentPage > 1) { %>
                    <a href="/com_list/search?query=<%= query %>&page=<%= currentPage - 1 %>">Previous</a>
                <% } %>
            
                <% for (let i = 1; i <= totalPages; i++) { %>
                    <a href="/com_list/search?query=<%= query %>&page=<%= i %>" class="<%= currentPage === i ? 'active' : '' %>">
                        <%= i %>
                    </a>
                <% } %>
            
                <% if (currentPage < totalPages) { %>
                    <a href="/com_list/search?query=<%= query %>&page=<%= currentPage + 1 %>">Next</a>
                <% } %>
            </div>            
        </div>
    

</body>
</html>